import { StyleSheet } from "react-native";

export const S = StyleSheet.create({
  btn: { position: "absolute", bottom: 20, right: 20 },
  txt: {
    backgroundColor: "#0F52BA",
    paddingHorizontal: 30,
    paddingVertical: 15,
    color: "white",
    borderRadius: 5,
  },
});
